# ============================================
# CONFIGURACIÓN DE SUPABASE
# ============================================
# Credenciales públicas de Supabase (seguras de exponer)
VITE_SUPABASE_PROJECT_ID="tu-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="tu-publishable-key"
VITE_SUPABASE_URL="tu-supabase-url"

# ============================================
# CONFIGURACIÓN DE BLOCKCHAIN (WAGMI + RAINBOWKIT)
# ============================================

# RPC URL para Polygon Amoy Testnet
# IMPORTANTE: Usar endpoints públicos o privados en .env.local
# NO compartir claves de API privadas en el repositorio
VITE_POLYGON_AMOY_RPC="https://polygon-rpc.com"

# WalletConnect Project ID (obtener en https://cloud.walletconnect.com)
# Requerido para conexiones de wallet avanzadas
VITE_WALLETCONNECT_PROJECT_ID="tu-wallet-connect-project-id"

# ============================================
# CONFIGURACIÓN DE DESARROLLO (HARDHAT)
# ============================================

# Private key del deployer (SOLO en .env.local, NUNCA en repositorio)
DEPLOYER_PRIVATE_KEY="tu-private-key-aqui"

# API Key de PolygonScan (opcional, para verificar contratos)
POLYGONSCAN_API_KEY="tu-polygonscan-api-key"

# RPC avanzada de Alchemy para Polygon Amoy (opcional)
# Usa esto solo en desarrollo, en .env.local
VITE_POLYGON_AMOY_RPC_ALCHEMY="https://polygon-amoy.g.alchemy.com/v2/tu-key"

# ============================================
# HEDERA HASHGRAPH (HCS — Immutable Anchoring)
# ============================================

# Hedera network: "testnet" (default) or "mainnet"
VITE_HEDERA_NETWORK="testnet"

# HCS Topic ID (create with: hcli topic create --memo "HarvestLink Protocol")
VITE_HEDERA_TOPIC_ID="0.0.XXXXXXX"

# Operator account ID (pays for HCS transactions)
VITE_HEDERA_OPERATOR_ID="0.0.XXXXXXX"

# ⚠️  SECURITY: VITE_ prefix exposes this to the browser bundle.
# ✅  OK for:   Local dev, testnet demos, hackathons.
# ❌  NOT OK for: Production with real funds / mainnet.
#
# PRODUCTION PATH → Supabase Edge Function:
#   1. Remove this variable (or set to "")
#   2. Store the real key as a Supabase secret (no VITE_ prefix):
#        supabase secrets set HEDERA_OPERATOR_KEY=<your-ed25519-key>
#   3. Implement supabase/functions/anchor-pack/index.ts
#      (calls HCS server-side; key never reaches browser)
VITE_HEDERA_OPERATOR_KEY=""

# Set to "true" ONLY for hackathon/demo deployments where client-side
# submission is intentionally used with testnet credentials.
# Never set in mainnet production.
VITE_HEDERA_ALLOW_CLIENT_SIDE="false"

# Smart contract address on Polygon Amoy (set after npm run contracts:deploy)
VITE_REGISTRY_CONTRACT_ADDRESS=""

# ============================================
# NOTAS DE SEGURIDAD
# ============================================
# 1. Variables prefijadas con VITE_ están disponibles en el cliente (frontend)
# 2. NO incluir claves privadas en variables VITE_* — excepción documentada arriba
# 3. Usar .env.local para datos sensibles (incluido en .gitignore)
# 4. Cambiar VITE_POLYGON_AMOY_RPC al endpoint privado en producción
# 5. Revisar que ninguna private key esté committeada en git
# 6. HEDERA_OPERATOR_KEY en producción → mover a Supabase Edge Function
